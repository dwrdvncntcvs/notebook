import React from "react";

const CreateNotes = () => {
    return (
        <section>
            <textarea
                name=""
                id=""
                placeholder="Enter notes here..."
            ></textarea>
            <button>Add</button>
        </section>
    );
};

export default CreateNotes;
